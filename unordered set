#include <bits/stdc++.h>
using namespace std;

int main() {
    unordered_set<int>st;
   st.insert(1);
   st.insert(5);
   st.insert(6);
   st.insert(10);
   st.insert(11);
   st.insert(13);
   for(auto it=st.begin(); it!=st.end(); it++){
       cout<<*it<<" ";
      
   }
	return 0;
}